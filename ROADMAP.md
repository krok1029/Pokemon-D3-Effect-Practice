# 後續工作建議

整理日期：2026-09-19。這是一份依現有程式碼提出的開發順序，所有項目均尚未實作。本次只整理文件；程式、測試、測試設定、fixture 與依賴保持不變。

近期目標：讓現有圖鑑與圖表能正確說明資料，並完成從探索到個別資料的瀏覽流程。先修正會讓使用者誤讀資料的問題，再補頁面入口與互動，最後擴充分析。

## 第一階段：修正資料正確性

### 1. 修正攻擊相剋倍率（優先）

**現況與原因**：`pokemonDetailViewModel.ts` 的 `buildOffenseMatchups` 用 `1` 作為取最大值的初值，所有小於 1 的倍率都被抬成 1。這會直接讓詳細頁顯示錯誤資訊。

**建議工作**：改用實際候選倍率計算最佳值，保留空屬性輸入的處理；清楚標示這是「自身屬性招式對單一目標屬性的最佳倍率」，並不包含特性、道具或實際招式配置。修正時可把相剋矩陣與純計算移到 Domain，ViewModel 保留排序、標籤與顏色；避免擴大成整個架構重寫。

**完成條件**：依現有相剋矩陣手動核對，一般屬性對幽靈為 0×、火對水為 0.5×、火對草為 2×；雙屬性取兩種攻擊中的較高值。列表與雙環圖顯示一致，防禦端原有倍率不受影響。

**相關位置**：`src/app/pokemon/view-models/pokemonDetailViewModel.ts`、`src/app/pokemon/[id]/page.tsx`。

### 2. 分清圖鑑編號與型態識別

**現況與原因**：1,032 筆 CSV 資料共用 898 個編號；詳細頁只以編號找第一筆，因此從 Mega 卡片點進去可能顯示一般型態。

**建議工作**：保留圖鑑編號作為物種資訊，另定穩定的型態識別，貫穿 Entity、DTO、ViewModel、列表連結與詳細頁。建議基本網址仍能指向預設型態，指定型態則使用額外路徑或參數；實作前先定義命名及舊網址行為。只使用 CSV 列索引作為識別會受資料重排影響，應避免。

**完成條件**：一般噴火龍、Mega Charizard X、Mega Charizard Y 可分別開啟、重新整理及複製網址，顯示的名稱、屬性、能力與卡片一致。既有 `/pokemon/6` 有明確的預設型態行為；不存在的型態回傳適當的找不到資料畫面。

**統計口徑**：近期建議維持「每列型態各算一筆」，先在畫面明示資料筆數與不同圖鑑編號數；若之後要提供基本型態統計，應新增明確篩選，不要直接依編號取第一筆去重。

### 3. 修正圖片對應

**現況與原因**：`pokemonImages.ts` 只取檔名前三位數，1000 以上會被誤判；同編號多張圖又只保留第一張，會影響型態與圖片的一致性。四位數問題也可能污染三位數編號的圖片索引。

**建議工作**：解析完整編號並比對檔名分隔符；接續型態識別設計，提供明確的型態圖片對照與缺圖降級規則。先盤點現有圖檔，不因圖片較新就假設 CSV 已涵蓋同樣範圍。

**完成條件**：三位與四位編號不衝突；同編號不同型態不靠檔案列舉順序決定圖片；沒有專用型態圖時採用明確標示的預設圖或佔位內容。只修圖片解析不代表已新增相應寶可夢資料。

## 第二階段：完成網站入口與基本瀏覽

### 4. 補首頁與小螢幕導覽

**現況與原因**：`/` 內容空白；導覽列的頁面連結在小螢幕隱藏，沒有替代入口；全域 metadata 仍是範本內容，頁尾部分連結沒有對應頁面。

**建議工作**：首頁簡短說明網站用途、資料範圍，提供「瀏覽圖鑑」與「探索圖表」兩個入口；補上小螢幕導覽，設定網站標題與描述，移除或完成沒有內容的頁尾入口。

**完成條件**：使用者從 `/` 開始，在桌面與手機寬度都能進入圖鑑及圖表並返回；主要導覽沒有死連結；統計數字標示為筆數／型態樣本，避免誤稱為不同物種數。

### 5. 讓大量卡片容易瀏覽

**現況與原因**：圖鑑會建立並渲染完整符合條件的列表；全部資料有 1,032 筆。搜尋、屬性等狀態不在網址，刷新或分享後無法還原。

**建議工作**：先實際啟動檢查載入與捲動，再選擇簡單分頁或「載入更多」；把關鍵字、屬性與傳說篩選同步到網址。將列表所需欄位與詳細頁的相剋計算分離，詳細頁只整理目標資料。Repository 的讀取中 Promise 共用可在量測確認後一併處理。

**完成條件**：篩選後的總筆數與當頁筆數清楚，改條件會重設到有效頁；網址可以重現篩選；從詳細頁返回仍能找到原本瀏覽位置。不先引入資料庫或新狀態管理套件。

## 第三階段：串起探索流程

### 6. 從散佈圖進入個別資料

**現況與原因**：圖表能框選資料並顯示結果，但它與圖鑑仍是兩個獨立的探索入口。

**建議工作**：在框選結果加入對應型態的詳細頁連結，統一名稱、屬性標籤與圖示；必要時把兩頁共用的呈現資訊抽到共用模組。

**依賴**：先完成型態識別，避免新連結延續現有的型態混淆。

**完成條件**：從散佈圖選中一隻寶可夢，可開啟正確型態與能力資料；圖例清楚標示按「主屬性」篩選，與圖鑑的主副屬性篩選差異可理解。

## 第四階段：再選一項擴充

完成前三階段後，依使用回饋選一項，不同時展開：

| 候選 | 使用價值 | 前置工作 |
| --- | --- | --- |
| 2～4 隻寶可夢比較 | 並排能力及雷達圖，讓查詢結果可以進一步比較 | 型態識別、跨頁選擇方式 |
| 世代與基本型態篩選 | 看不同群體的能力分布 | 將 CSV 現有欄位納入模型，定義型態分類 |
| 中文名稱搜尋 | 降低目前只能以資料中的英文名稱搜尋的門檻 | 可維護的名稱對照與來源；不能只把圖片檔名當成完整翻譯資料 |

API / 資料庫、更完整戰鬥模擬、帳號收藏及全面升級依賴，目前沒有足夠需求支持優先投入。

## 開工與驗收方式

第一個開發批次建議只處理「攻擊相剋倍率」，完成後再處理型態與圖片，方便逐項確認行為。開工前先記錄 Node.js / Yarn 版本，啟動 `/chart`、`/pokemon` 與一個詳細頁，建立實際運作基線。

依目前要求，測試維護暫不排入這批工作，不新增、修改或刪除測試、fixture、測試設定。本次列出的完成條件可以先供手動驗收；日後實作時可執行既有檢查並如實記錄結果，若測試與預期變更衝突，再另行說明，不直接改寫測試預期。
