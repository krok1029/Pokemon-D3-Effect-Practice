# 專案協作規則

接手時先閱讀 `agent.md`、`README.md` 與 `ROADMAP.md`，並確認目前工作目錄的修改。

## 多任務提交

- 多個任務同時作業時，依不同任務或變更目的分別建立 commit，不將無關內容合併提交。
- 同一功能的程式、必要測試與驗收文件放在同一個 commit，讓每筆提交可獨立理解。
- 提交前逐一確認檔案與差異，只暫存該任務的變更；共用檔案含多項變更時，依內容分段暫存。
- 保留使用者與其他任務的既有修改；先協調共用檔案，再執行暫存、提交與推送，避免同時操作 Git index。
- 推送前檢查待推送的 commit 清單；除非使用者明確要求，不合併或改寫已完成的提交。
